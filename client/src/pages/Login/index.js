import React from "react";
import Layout from "../../components/Layout"

const Login = () => {
    return (
        <Layout title="Login">
            <div>
                <h1>Login</h1>
            </div>
        </Layout>
    );
};

export default Login;